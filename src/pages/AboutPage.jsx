function AboutPages() {
  
  return (

    <div>
      <h2>About Dog Explorer</h2>
      <p>
        This simple React app demonstrates fetching and paginating data from an
        external API. It shows a list of dog breeds and allows the user to
        navigate through pages of results.
      </p>
      <p>
        Built as a learning project, it uses React Router for client-side
        navigation and basic components for structure.
        {true && 5}
      </p>
    </div>
  );
}

export default  AboutPages;
